name: CI

on:
  push:
    branches: ["main", "develop", "staging", "release/**"]
  pull_request:
    branches: ["main", "develop"]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Show structure
        run: |
          echo "=== Repository Structure ==="
          find . -maxdepth 3 -type f -name "*.md" | head -20
          echo ""
          echo "=== Docker Compose ==="
          cat docker-compose.yml || true

      # TODO: Add actual linting when code is added
      - name: Lint placeholder
        run: echo "TODO - Add linting steps"

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: lint
    
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: akistel
          POSTGRES_PASSWORD: akistel
          POSTGRES_DB: akistel_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      
      redis:
        image: redis:7
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4
      
      # TODO: Add actual test steps when code is added
      - name: Test placeholder
        run: echo "TODO - Add test steps"
        env:
          DATABASE_URL: postgresql://akistel:akistel@localhost:5432/akistel_test
          REDIS_URL: redis://localhost:6379

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: test
    
    steps:
      - uses: actions/checkout@v4
      
      # TODO: Add actual build steps
      - name: Build placeholder
        run: echo "TODO - Add build steps"
